---
title: "Making Your First Call to the Scratchie Open API"
api: "GET https://sandbox.api.scratchie.com/me"
description: "Now that we have successfully retrieved an access token, we can use it to make our first call to the Scratchie Open API. For this example, we’ll use the simple /me endpoint to show that we can successfully contact the Scratchie Open API server and return information about the currently logged in user."
---

### Headers

<ParamField body="Authorization" type="String">
  Bearer `<access_token>`
</ParamField>

### Response

<ResponseField name="email" type="String">
  The email of the currently logged in user.
</ResponseField>

<ResponseField name="Id" type="Number">
  The unique ID of the user.
</ResponseField>

<ResponseField name="firstName" type="String">
  The first name of the user.
</ResponseField>

<ResponseField name="lastName" type="String">
  The last name of the user.
</ResponseField>

<RequestExample>

```bash
curl --location --request GET 'https://{BASE_URL}/me' \
--header 'Authorization: Bearer <access_token>'

```

</RequestExample>

<ResponseExample>

```json Response
{
  "email": "scratchieopentest123@gmail.com",
  "Id": 878978,
  "firstName": "scratchie",
  "lastName": "test"
}
```

</ResponseExample>
